{
  "name": "acta",
  "version": "0.1.0",
  "description": "A tamper-evident record of what an agent actually did \u2014 hash-chained, signed, anchored, and honest about what that does not prove.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "bin": {
    "acta": "bin/acta.mjs"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "lint": "tsc --noEmit",
    "attack": "node --experimental-strip-types scripts/attack.ts"
  },
  "engines": {
    "node": ">=22.6"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "~5.8.2"
  }
}
